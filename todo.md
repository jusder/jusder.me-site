# Website Redesign Notes

## Current Direction

- Favorite direction is mockup #2, the clean "Product Shelf" look.
- #3 is the second favorite direction and may be useful as inspiration if #2 needs more warmth or personality.
- Keep the visible brand as `jusder`, not `jus.der`.
- Use `jusder.me` where it reads as an address or handle, such as footer/contact/social.
- Keep the Instagram link visible in the main navigation.

## Staged Files

- `homepage-mockups.html` contains the exploratory mockups.
- `index-v2.html` is the complete #2 candidate page.
- `index.html` is still untouched and should remain the live homepage until cutover.

## Copy To Revisit

- Current staged tagline:
  "Small, thoughtful apps for travel, local life, planning, and everyday tools. Built by Rich in Denver."
- The direction feels clean, but the copy may need more personality or a sharper point of view before cutover.
- Consider whether the hero should emphasize:
  - independent apps
  - practical tools for real routines
  - travel/local life/planning as the main categories
  - Rich as the maker/founder
  - Denver as context, not the whole identity

## Cutover Reminder

- When ready, replace `index.html` with `index-v2.html`.
- Recheck links, responsive layout, and metadata after the cutover.
- Commit and push only after the final copy and layout feel settled.
